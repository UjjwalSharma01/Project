module.exports = (on, config) => {
  // You can add custom plugins here
  // For example, you can require additional plugins or modify the Cypress configuration

  // Return the config object
  return config;
};